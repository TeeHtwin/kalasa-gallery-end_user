import { Inter, Cardo } from 'next/font/google'
 
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})
 
export const cardo = Cardo({
  subsets: ['latin'],
  display: 'swap',
  weight : ['400','700']
})